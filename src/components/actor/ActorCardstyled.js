import styled from 'styled-components';
import { SearchCard } from '../styles';

export const StyledActorCard = styled(SearchCard)`::after
    .deathday {
        margin: 0;
        margin-top: 15px;
        font-weight: bold;
    }
`;